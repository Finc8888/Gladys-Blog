# SSL Configuration for GladysAI-Blog
# Copy this file to .env and update with your values

# Domain configuration
# Replace with your actual domain name
DOMAIN=localhost
# For production, use your real domain:
# DOMAIN=yourdomain.com

# Email for Let's Encrypt notifications
# Replace with your actual email address
EMAIL=admin@localhost
# For production, use your real email:
# EMAIL=your-email@example.com

# Let's Encrypt staging environment
# Set to 1 for testing (staging certificates)
# Set to 0 for production (real certificates)
STAGING=0

# Force certificate renewal
# Set to 1 to force renewal of existing certificates
# Set to 0 for normal operation
FORCE_RENEWAL=0

# Fallback to self-signed certificates
# Set to 1 to generate self-signed certs if Let's Encrypt fails
# Set to 0 to fallback to HTTP-only mode instead
FALLBACK_SELFSIGNED=1

# Nginx configuration
NGINX_WORKER_PROCESSES=auto
NGINX_WORKER_CONNECTIONS=1024

# Docker Compose profiles
# Uncomment the profiles you want to use:

# For automatic certificate renewal (recommended for production)
# COMPOSE_PROFILES=auto-renew

# For SSL monitoring
# COMPOSE_PROFILES=monitoring

# For using nginx proxy (advanced setup)
# COMPOSE_PROFILES=proxy

# For manual certificate generation
# COMPOSE_PROFILES=manual-cert

# Multiple profiles (comma-separated)
# COMPOSE_PROFILES=auto-renew,monitoring

# Development settings
# Set to development for dev environment
# HUGO_ENV=development

# Production deployment settings
# COMPOSE_PROJECT_NAME=gladys-blog-ssl

# SSL Certificate paths (if using bind mounts instead of volumes)
# SSL_CERT_PATH=./ssl/letsencrypt
# SSL_WEBROOT_PATH=./ssl/certbot

# Logging configuration
# LOG_DRIVER=json-file
# LOG_MAX_SIZE=10m
# LOG_MAX_FILE=3

# Resource limits
# MEMORY_LIMIT=512M
# CPU_LIMIT=1.0
# MEMORY_RESERVATION=256M
# CPU_RESERVATION=0.5

# Health check configuration
# HEALTH_CHECK_INTERVAL=30s
# HEALTH_CHECK_TIMEOUT=10s
# HEALTH_CHECK_RETRIES=3
# HEALTH_CHECK_START_PERIOD=30s

# Network configuration
# NETWORK_NAME=gladys-blog-ssl-network

# Security settings
# Set to 1 to enable additional security headers
SECURITY_HEADERS=1

# Rate limiting
# RATE_LIMIT_GENERAL=10r/s
# RATE_LIMIT_API=5r/s

# Cache settings
# CACHE_STATIC_FILES=1y
# CACHE_HTML_FILES=10m
# CACHE_API_RESPONSES=5m

# Custom SSL settings (advanced)
# SSL_PROTOCOLS=TLSv1.2 TLSv1.3
# SSL_CIPHERS=ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512
# SSL_SESSION_TIMEOUT=10m

# Backup configuration
# BACKUP_ENABLED=1
# BACKUP_SCHEDULE=0 2 * * *
# BACKUP_RETENTION_DAYS=30

# Monitoring and alerting
# MONITORING_ENABLED=0
# ALERT_EMAIL=monitoring@yourdomain.com
# SLACK_WEBHOOK_URL=

# Development overrides
# For local development, you might want to override some settings:
# DEV_PORT_HTTP=8080
# DEV_PORT_HTTPS=8443
# DEV_DOMAIN=localhost
# DEV_SSL_VERIFY=0
